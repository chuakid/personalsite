---
import { getCollection } from "astro:content";
import Layout from "../layouts/Layout.astro";
import Project from "../components/project.astro";
import Information from "../components/information.astro";
import Experience from "../components/experience.astro";

const projects = await getCollection("projects");
const experiences = await getCollection("experiences");
---

<Layout title="Kelvin Chua">
  <main
    class="p-8 sm:m-12 md:flex md:items-start min-h-screen text-white mx-auto mt-10"
  >
    <Information />

    <section class="flex-grow">
      <section id="experiences">
        <h2 class="text-5xl mb-4 text-blue-200">Experience</h2>
        {
          experiences.map((experience) => (
            <Experience experience={experience} />
          ))
        }
      </section>
	  
      <section id="projects">
        <h2 class="text-5xl mb-4 text-blue-200">Projects</h2>
        <div class="section">
          {projects.map((project) => <Project project={project} />)}
        </div>
      </section>
    </section>
  </main>
</Layout>
