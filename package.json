{
  "name": "personal-site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {},
  "devDependencies": {
    "autoprefixer": "^10.3.4",
    "cssnano": "^5.0.8",
    "postcss": "^8.3.6",
    "postcss-cli": "^8.3.1",
    "save-dev": "0.0.1-security",
    "tailwindcss": "^2.2.15",
    "@11ty/eleventy": "^0.12.1"
  },
  "scripts": {
    "tailwind:process": "npx tailwindcss -i src/style.css -o _site/assets/css/style.css",
    "dev": "rm -rf _site && concurrently \"npm run tailwind:process -- -w\" \"eleventy --serve --quiet\"",
    "build": "rm -rf _site && NODE_ENV=production npm run tailwind:process -- --minify && eleventy"
  },
  "author": "",
  "license": "ISC"
}